<div class="flex flex-col justify-center items-center gap-3 min-h-screen">
  <%= render partial: "metric", locals: { metric: @metric } %>
  <%= button_to "Delete metric", @metric, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "bg-white rounded-lg shadow-lg transition-transform transform hover:scale-110 w-64"%>
</div>
