<!DOCTYPE html>
<html>
  <head>
    <title>RunningTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="prose min-h-screen">
    <%= render partial: "shared/header" %>

    <% if notice %>
      <p class="w-[300px] mx-auto bg-green-400 border-solid border-2 border-green-500 rounded-md p-2 text-center text-white"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="w-[300px] mx-auto bg-red-300 border-solid border-2 border-red-400 rounded-md p-2 text-center text-white"><%= alert %></p>
    <% end %>
          
    <%= yield %>
  </body>
</html>
